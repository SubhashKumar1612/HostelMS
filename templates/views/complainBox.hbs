<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Complaint Box</title>
    <link rel="stylesheet" href="css/complaintbox.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=K2D:wght@100;200;300;400;500;600;700;800&display=swap" rel="stylesheet" />
</head>
<body>
    {{>nav}}
    <main>
        <nav>
            <div class="intro">
                <div class="greeting">
                    <div class="identity">
                        <h3 class="welcome" id="msg">Welcome,</h3>
                        <p class="welcome" id="userid">{{user.name}}</p>
                    </div>
                </div>
                <div class="greeting1">
                    <div class="identity1">
                        <button class="welcome" id="Logoutbtn">Logout</button>
                    </div>
                </div>
            </div>
        </nav>
        <section>
            <div class="spacing"></div>
            <div class="content">
                <div class="heading"><p>Complaints:</p></div>
                <form action="/complainBox" method="POST">
                    <div class="row">
                        <div class="col category">
                            <p>Category:</p>
                        </div>
                        <div class="col">
                            <select name="category" required>
                                <option value="Rooms Related">Rooms Related</option>
                                <option value="Food Related">Food Related</option>
                                <option value="Canteen">Canteen</option>
                                <option value="Fees">Fees</option>
                                <option value="Other">Other</option>
                            </select>
                        </div>
                    </div>
                    <div class="message">
                        <textarea name="message" id="message" cols="30" rows="10" placeholder="Type your queries here:" required></textarea>
                    </div>
                    <div class="Buttons">
                        <button type="submit" class="contact"><p>Submit</p></button>
                    </div>
                </form>
            </div>
            <div class="spacing"></div>
        </section>
    </main>
</body>
</html>
